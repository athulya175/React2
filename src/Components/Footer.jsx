function Footer(){
    return(
        <footer>
            <p>&copy;{new Date().getFullYear()} your wesite name</p> {/**adds a js to get the date if we are adding a js it should be inside the {} */}
        </footer>
    )
}
export default Footer